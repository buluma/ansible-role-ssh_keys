---
#
# Ansible managed
#
argument_specs:
  main:
    short_description: Manage ssh public key authentication (public / private / authorized keys and known hosts) in Debian-like systems
    description: >
      Manage ssh public key authentication (public / private / authorized keys and known hosts) in Debian-like systems
    author: Michael Buluma
